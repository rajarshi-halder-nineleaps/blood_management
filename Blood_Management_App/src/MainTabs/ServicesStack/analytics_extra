<Text>
              Find Now!
            </Text>
            <LineChart
              data={data}
              width={screenWidth}
              height={256}
              verticalLabelRotation={30}
              chartConfig={chartConfig}
              bezier
            />
            <BarChart
              style={styles.graphStyle}
              data={data}
              width={screenWidth - 30}
              height={220}
              yAxisLabel="Rs"
              chartConfig={chartConfig}
              verticalLabelRotation={30}
            />

